# Python 3.8.2 or above 
# pip 20.1. or above
# you must have the above installed in your system to run AutoMailer 
# you can then go to terminal / command prompt 
# navigate to the directory where AutoMailer is installed
# and execute 
# pip install -r requirements.txt
# the above command will automatically install the following dependancies
# only PyYaml is essential 
astroid==2.4.2
isort==4.3.21
lazy-object-proxy==1.4.3
mccabe==0.6.1
pylint==2.5.3
PyYAML==5.3.1 # essential
six==1.15.0
toml==0.10.1
wrapt==1.12.1
